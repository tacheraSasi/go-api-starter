/**
 * Keys for to identify localStorage
 */
enum STORAGE_KEYS {
  AUTH = 'app/auth',
  LANGUAGE = 'app/locale',
  THEME = 'app/theme',
}

const SHOW_DEV_TOOLS = true;

export { STORAGE_KEYS, SHOW_DEV_TOOLS };
